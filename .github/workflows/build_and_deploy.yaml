name: Build and Deploy
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Build
        run: echo "Building..."

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Print Deploy
        run: echo "Deploying..."
